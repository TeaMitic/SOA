{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/MicroService-DB/package.json",
                "localRoot": "${workspaceFolder}/MicroService-DB"
            }
        }
    ]
}